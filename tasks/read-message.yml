---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: concourse/buildroot
    tag: 'curl'

inputs:
- name: telegram1

run:
  path: sh
  args:
  - -exc
  - |
    whoami
    env
    cat telegram1/message
